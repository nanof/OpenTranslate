<Window x:Class="OpenTranslate.Views.HotkeyCaptureDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Capture shortcut"
        Width="320"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource ThemeWindowBg}"
        PreviewKeyDown="OnPreviewKeyDown">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Press the key combination you want to use."
                   Foreground="{DynamicResource ThemeForeground}"
                   FontSize="12"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12" />

        <Border Grid.Row="1"
                Padding="10,8"
                Background="{DynamicResource ThemeSurface}"
                BorderBrush="{DynamicResource ThemeBorder}"
                BorderThickness="1"
                Margin="0,0,0,12">
            <TextBlock x:Name="PreviewText"
                       Text="Waiting…"
                       Foreground="{DynamicResource ThemeAccent}"
                       FontSize="14"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center" />
        </Border>

        <Button Grid.Row="2"
                Content="Cancel"
                HorizontalAlignment="Right"
                Padding="12,5"
                Background="{DynamicResource ThemeSurfaceHover}"
                Foreground="{DynamicResource ThemeForeground}"
                BorderThickness="0"
                FontSize="11"
                Click="OnCancel" />
    </Grid>
</Window>
